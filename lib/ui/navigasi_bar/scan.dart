import 'package:flutter/material.dart';

class Scan extends StatefulWidget {
  @override
  SscanState createState() => SscanState();
}

class SscanState extends State<Scan> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Scan")),
    );
  }
}
